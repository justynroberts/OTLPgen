# Local dev build (the cross-compiled dist/ binaries ARE committed for releases)
/otlpgen
*.out
.DS_Store

# Local run helpers that hold live credentials — never commit these.
# A sanitized template lives in examples/grafana.yaml.
/dist/run.sh
/dist/grafana.yaml
